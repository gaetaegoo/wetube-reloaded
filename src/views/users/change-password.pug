extends ../base.pug

block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(name="oldPassword", type="password", placeholder="Old Password")
        input(name="newPassword", type="password", placeholder="New Password")
        input(name="newPassword2", type="password", placeholder="New Password Confirmation")
        button Change Password